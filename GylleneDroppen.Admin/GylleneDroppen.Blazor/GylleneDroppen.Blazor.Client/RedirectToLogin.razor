@inject NavigationManager NavigationManager

@code {

    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo($"konto/logga-in?returnUrl={Uri.EscapeDataString(NavigationManager.Uri)}", forceLoad: true);
    }

}